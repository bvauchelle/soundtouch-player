# SoundTouch player
A web UI to allow playing streams on Soundtouch devices after the Bose SoundTouch cloud was terminated. To be hosted on the same local network as the SoundTouch devices so they can access the server url.
